# Guessing-Game
A simples number guessing game in Java where the user has 5 attempts to guess a random number between 1 and 100, with hints indicating whether the guess is higher or lower than the correct number.
